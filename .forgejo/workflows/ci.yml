name: ci
on:
  push:
    branches: [main]
  pull_request:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build MyWeb
        working-directory: MyWeb
        run: |
          npm ci --no-audit --no-fund
          npm run build
